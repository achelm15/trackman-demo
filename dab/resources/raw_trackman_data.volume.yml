resources:
  volumes:
    raw_trackman_data:
      catalog_name: ${var.catalog}
      schema_name: ${resources.schemas.trackman.name}
      name: raw_trackman_data
      volume_type: MANAGED
      comment: 'Raw TrackMan data landing volume'
